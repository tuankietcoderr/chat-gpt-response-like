{
  "name": "gpt-response-like",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd server && yarn dev\" \"cd client && yarn dev\"",
    "install-client": "cd client && yarn install",
    "install-server": "cd server && yarn install",
    "install-all": "yarn install-client && yarn install-server && yarn"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
